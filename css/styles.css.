:root{
  --bg:#0b0d10;
  --panel:#11151b;
  --text:#eaf0ff;
  --muted:#b7c0d6;
  --accent:#7dd3fc;
  --line:rgba(255,255,255,.10);
  --shadow: 0 10px 30px rgba(0,0,0,.35);
  --radius: 16px;
}

:root[data-theme="light"]{
  --bg:#f6f7fb;
  --panel:#ffffff;
  --text:#0d1321;
  --muted:#4b5563;
  --accent:#0ea5e9;
  --line:rgba(0,0,0,.10);
  --shadow: 0 10px 30px rgba(17,24,39,.12);
}

*{box-sizing:border-box}
html{scroll-behavior:smooth}
body{
  margin:0;
  font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  background: radial-gradient(1200px 700px at 20% 0%, rgba(125,211,252,.18), transparent 55%),
              radial-gradient(900px 600px at 80% 10%, rgba(56,189,248,.14), transparent 55%),
              var(--bg);
  color:var(--text);
}

a{color:inherit; text-decoration:none}
code{background: rgba(125,211,252,.12); padding: .1rem .35rem; border-radius: .4rem}

.container{width:min(1100px, calc(100% - 2rem)); margin-inline:auto}
.section{padding: 72px 0}
.section.alt{background: linear-gradient(180deg, transparent, rgba(255,255,255,.02), transparent)}
h1,h2,h3{line-height:1.1; margin:0 0 .75rem}
h1{font-size: clamp(2.2rem, 4vw, 3.2rem)}
h2{font-size: clamp(1.6rem, 2.5vw, 2.0rem)}
p{color: var(--muted); font-size: 1.05rem; line-height:1.6}
p.lede{font-size:1.15rem; max-width: 60ch}
p.tight{margin:.35rem 0}
p.note{margin-top: 1rem; font-size: .95rem}

.skip{
  position:absolute; left:-999px; top:0;
  background:var(--panel); border:1px solid var(--line);
  padding:.6rem .9rem; border-radius:.6rem;
}
.skip:focus{left:1rem; top:1rem; z-index:9999}

.site-header{
  position:sticky; top:0; z-index:20;
  backdrop-filter: blur(10px);
  background: color-mix(in srgb, var(--bg) 70%, transparent);
  border-bottom: 1px solid var(--line);
}

.nav{
  display:flex; align-items:center; justify-content:space-between;
  gap: 1rem;
  padding: 14px 0;
}
.brand{display:flex; align-items:center; gap:.6rem; font-weight:800}
.brand-dot{width:10px; height:10px; border-radius:99px; background:var(--accent); box-shadow:0 0 22px rgba(125,211,252,.45)}
.nav-links{display:flex; align-items:center; gap: .9rem}
.nav-links a{color: var(--muted); font-weight:600}
.nav-links a:hover{color: var(--text)}
.theme-btn{
  border:1px solid var(--line); background:var(--panel); color:var(--text);
  border-radius: 999px; padding:.45rem .6rem; cursor:pointer;
}
.nav-toggle{
  display:none; border:1px solid var(--line); background:var(--panel); color:var(--text);
  border-radius: 999px; padding:.45rem .7rem; cursor:pointer;
}
.sr-only{position:absolute; width:1px; height:1px; padding:0; margin:-1px; overflow:hidden; clip:rect(0,0,0,0); border:0}

.hero{padding: 78px 0 36px}
.hero-grid{
  display:grid;
  grid-template-columns: 1.35fr .9fr;
  gap: 1.4rem;
  align-items:start;
}
.kicker{
  letter-spacing:.14em; text-transform:uppercase; font-weight:800;
  color: color-mix(in srgb, var(--accent) 85%, var(--muted));
  margin:0 0 .8rem;
}
.accent{color: var(--text); text-shadow: 0 0 22px rgba(125,211,252,.15)}
.card{
  background: color-mix(in srgb, var(--panel) 92%, transparent);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  padding: 1.1rem 1.1rem;
  box-shadow: var(--shadow);
}
.hero-card h2{font-size:1.1rem; margin-bottom:.6rem}

.bullets{margin:.65rem 0 0; padding-left: 1.1rem; color:var(--muted)}
.bullets li{margin:.35rem 0}

.btn{
  display:inline-flex; align-items:center; justify-content:center;
  border:1px solid var(--line); background:var(--panel);
  padding: .75rem 1rem; border-radius: 999px;
  font-weight: 700;
}
.btn:hover{transform: translateY(-1px)}
.btn.primary{
  background: linear-gradient(135deg, rgba(125,211,252,.30), rgba(14,165,233,.25));
  border-color: rgba(125,211,252,.35);
}
.cta-row{display:flex; gap:.75rem; flex-wrap:wrap; margin: 1.2rem 0}
.meta-row{display:flex; gap:.6rem; flex-wrap:wrap}
.pill{
  border:1px solid var(--line);
  padding:.45rem .7rem;
  border-radius: 999px;
  color: var(--muted);
  background: color-mix(in srgb, var(--panel) 88%, transparent);
  font-weight: 600;
}

.two-col{
  display:grid;
  grid-template-columns: 1.1fr .9fr;
  gap: 1rem;
  margin-top: 1rem;
}

.section-head{display:flex; align-items:flex-end; justify-content:space-between; gap: 1rem}

.filters{display:flex; gap:.5rem; flex-wrap:wrap; margin: 1rem 0 1.25rem}
.chip{
  border:1px solid var(--line);
  background: color-mix(in srgb, var(--panel) 88%, transparent);
  color: var(--muted);
  padding: .45rem .75rem;
  border-radius: 999px;
  cursor:pointer;
  font-weight:700;
}
.chip.active{color:var(--text); border-color: rgba(125,211,252,.35)}

.grid{
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
}
.project h3{margin-bottom:.25rem}
.project .tags{display:flex; gap:.4rem; flex-wrap:wrap; margin-top:.8rem}
.tag{
  font-size:.85rem;
  border:1px solid var(--line);
  padding:.2rem .55rem;
  border-radius: 999px;
  color: var(--muted);
}

.timeline{display:grid; gap: 1rem; margin-top: 1rem}
.timeline-item{display:grid; grid-template-columns: 16px 1fr; gap: .9rem; align-items:start}
.timeline-dot{
  width:12px; height:12px; border-radius: 99px; margin-top: 1.2rem;
  background: var(--accent); box-shadow: 0 0 18px rgba(125,211,252,.35);
}

.skill-grid{
  display:grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1rem;
  margin-top: 1rem;
}

.contact-form label{display:block; margin:.8rem 0; color: var(--muted); font-weight:700}
.contact-form input, .contact-form textarea{
  width:100%;
  margin-top:.4rem;
  border:1px solid var(--line);
  background: color-mix(in srgb, var(--panel) 88%, transparent);
  color: var(--text);
  border-radius: 12px;
  padding: .75rem .85rem;
  font: inherit;
}
.footer{padding: 26px 0; border-top: 1px solid var(--line)}
.muted{color: var(--muted)}

.reveal{opacity:0; transform: translateY(10px); transition: opacity .6s ease, transform .6s ease}
.reveal.visible{opacity:1; transform:none}

@media (max-width: 900px){
  .hero-grid{grid-template-columns: 1fr}
  .two-col{grid-template-columns: 1fr}
  .grid{grid-template-columns: 1fr}
  .skill-grid{grid-template-columns: 1fr}
  .nav-toggle{display:inline-flex}
  .nav-links{
    position:absolute; right:1rem; top:60px;
    display:none; flex-direction:column; align-items:flex-start;
    width:min(320px, calc(100% - 2rem));
    padding: .8rem;
    background: var(--panel);
    border:1px solid var(--line);
    border-radius: var(--radius);
    box-shadow: var(--shadow);
  }
  .nav-links.open{display:flex}
}
